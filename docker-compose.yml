services:
  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    environment:
      PYTHONPATH: /app/backend
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - ./outputs/local_data:/app/outputs/local_data
